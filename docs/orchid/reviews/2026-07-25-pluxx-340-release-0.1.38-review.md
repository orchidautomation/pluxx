# PLUXX-340 Pluxx 0.1.38 Release Review

Date: 2026-07-25

## Scope

- Base: `b979bc6e02bb9801fa623f80302000553a0693c7`
- Reviewed release-prep head: `46105a0befa48c5838342ff2d0f1127b2aaa5b8d`
- Receipt-bound release-prep commit: `361958f6b16b1acc2bb901c97fc2c6d5a77ba880`
- Release: `@orchid-labs/pluxx@0.1.38` / `v0.1.38`
- Purpose: follow-up PLUXX-340 correction for duplicate release-manifest archive identities discovered after shipped and independently verified 0.1.37

## Gate Evidence

- Fail-first proof freshness rejected the 0.1.38 package bump while canonical proof still named 0.1.37.
- Recovery proof failed first when the 0.1.38 receipt IDs met the old version-specific recovery map.
- Focused proof, workflow, recovery, package, publish, and fake-home release suites passed.
- `npm run release:check` passed after the review fixes:
  - proof freshness: 20 claims and 20 receipts
  - build and typecheck
  - 62 test files and 816 tests
  - isolated packed Node runtime verification
  - npm dry-run package for `@orchid-labs/pluxx@0.1.38`

## Independent Review

Correctness, testing, maintainability, project-standards, agent-native, learnings, and adversarial reviewers inspected the release diff. The external cross-model adversarial pass was not run because the security approval layer rejected third-party repository-diff egress.

The first review found two P1 blockers:

1. Immutable-tag recovery recognized only the historical 0.1.37 receipt IDs.
2. A squash or rebase merge would make receipt commit `361958f6` unreachable from trusted `main`, causing proof freshness to fail before publication.

The release-prep branch now:

- chooses recovery command sets by supported proof tier rather than release-specific receipt ID;
- exercises recovery with the canonical package version and tag;
- rejects unsupported environment proof tiers;
- requires a true merge commit for receipt-bearing release PRs; and
- requires exact PR-head and receipt-commit ancestry checks before tagging.

Correctness and adversarial re-review reported zero current findings. Maintainability and project-standards review reported no findings. Agent-native review found no parity gap. No repository learning corpus existed to apply.

## Residual Release Gates

- Merge only the exact unchanged PR head with a true merge commit.
- Fetch trusted `main`, then prove the PR head and `361958f6b16b1acc2bb901c97fc2c6d5a77ba880` are ancestors of the exact merge commit.
- Confirm `v0.1.38` does not already exist before creating it once.
- Publish only through the trusted tag-triggered workflow.
- Independently verify tag provenance, npm and GitHub artifact byte identity and checksums, and installed CLI behavior.

## Verdict

Ready for the release PR with zero current review findings. Merge, tag, publication, and public artifact verification remain action-time gates.

## Immutable-Tag Recovery Addendum

Release PR [#462](https://github.com/orchidautomation/pluxx/pull/462) merged exact reviewed head `cbc14e007626328a8d25340419a455f40701426c` with true merge commit `e24d4db3f57fa0942376237fb212cb49368d7704`. Both that head and receipt-bound commit `361958f6b16b1acc2bb901c97fc2c6d5a77ba880` are trusted-main ancestors. Immutable tag `v0.1.38` points to the merge commit and has not moved.

Tag-triggered Release run [30152484521](https://github.com/orchidautomation/pluxx/actions/runs/30152484521) failed closed during `npm test` before pack or publication. The production-tag proof-checker test cloned the now-tagged repository with tags, then attempted to create `v0.1.38` again. The failure was reproduced locally only after the real tag existed.

The reviewed recovery change:

- makes the production proof-checker fixture clone without tags;
- runs recovery tests from a tagless clone whose exact commit must match the immutable release checkout;
- rebuilds that exact source clone before testing so release-smoke has its required generated `dist/`;
- strengthens the recovery fixture to fail when the version tag is inherited or build output is missing;
- cleans an independently exposed `alias-agent.md` shared-fixture leak in `finally`; and
- records the recovery-boundary truth that the tag existed while public 0.1.38 was still blocked.

Fail-first and gate evidence:

- the production proof-checker test reproduced the tag collision as status 128 after `v0.1.38` existed;
- adversarial review reproduced the missing-`dist/` blocker in an exact tagless `e24d4db3f57fa0942376237fb212cb49368d7704` clone;
- the full suite exposed the shared-fixture leak as a 815/816 failure before cleanup was added;
- focused build, recovery, workflow, and proof suites passed 83/83;
- the complete `npm run release:check` passed with 62 files, 816 tests, isolated packed Node runtime verification, and npm dry-run package `@orchid-labs/pluxx@0.1.38`;
- independent correctness and adversarial re-review reported zero current actionable findings; and
- adversarial re-review independently built the exact tagless release clone and passed release-smoke 10/10.

Recovery PR [#463](https://github.com/orchidautomation/pluxx/pull/463) merged unchanged reviewed head `4024ca07fe9cc78b98e93bb0931b8d91ddc36f0f` as true merge commit `a67803184890795457d095da52f4a243d61daf2a`. Release run [30154432818](https://github.com/orchidautomation/pluxx/actions/runs/30154432818) completed from that exact trusted-main commit while `v0.1.38` remained fixed at `e24d4db3f57fa0942376237fb212cb49368d7704`.

Independent post-publication verification confirmed npm and GitHub serve byte-identical 733,666-byte tarballs at SHA-256 `34af5bd24a441f13094b651658d6728509771a8cb9295f82b1bc66a985cb2c24`, npm SHA-1 `d21667fb161598e84bc10904596340506e5eba65`, and SRI `sha512-5bcnZpoZswwd878DWz60kPwk9ORPL+Z1L7J1E6II2yMyKLhhaRjWLjjEZ72x8ZCbC71lRM5uHv3wCdQQvamLOQ==`. The verified recovery receipt binds tag tree `8d876bac5753d0dab019f573b4f3bf806bddaa04`, trusted recovery main `a67803184890795457d095da52f4a243d61daf2a`, both current receipt IDs, and every required validation as passed. An isolated registry-installed CLI reported `0.1.38`.
