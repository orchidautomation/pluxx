---
title: PLUXX-329 Phase 6 Review
date: 2026-07-14
status: green-after-fixes
scope: frozen-core-four-release-gate
---

# PLUXX-329 Phase 6 Review

## Verdict

Phase 6 meets the frozen core-four release-gate contract. One hermetic `core-four:proof` command independently pins the exact three source identities, versions, revisions, aggregate inventory digests, executable modes, and 44 rows; validates all 12 orchestration/adjunct receipts; re-derives migration and generator compatibility from current registries; compares maintained generated output in memory; and replays clean symlink-root and copied-install preimages twice.

The unchanged evidence ceiling is enforced exactly: generated and installed proof 12/12, discovery environment-unavailable 12/12, activation unsupported 12/12, behavior environment-unavailable 12/12, and all 324 field outcomes degraded. Generated registration artifacts cannot satisfy discovery, activation, or behavior. Claude Code, Cursor, Codex, and OpenCode remain the only included hosts.

## CE Findings Incorporated

- P1: the 44-row inventory was self-authenticating because its enclosing digest was recomputed from mutable rows while only revision and count were independently pinned. Fixed by independently pinning plugin, version, revision, aggregate inventory digest, and count for all three fixtures, with a self-consistent mode-tamper negative.
- P2: the ownership digest omitted the logical install path, generated-root relationship, surface, receipt binding, and validated surface count. Fixed by hashing a normalized preimage after exact live copy/symlink validation and using the validator's returned kind/count in receipts.
- P2: the adversarial test title claimed mode and retarget coverage without exercising them. Fixed with explicit `0755 -> 0644` drift, existing wrong-target, dangling target, generated-registration promotion, path escape, nested-link, kind-substitution, collision, missing, and content negatives.
- P2: ownership tests changed `HOME` without restoring it and did not guarantee cleanup after assertion failure. Fixed with exact environment restoration and temp-root cleanup in `finally` blocks.
- P2: the named release gate did not directly re-derive migration compatibility. Fixed with a hermetic synthetic migration topology whose exact nine IDs, content digests, executable modes, deterministic provenance, and complete 9x4 outcome-mode matrix are pinned against current registry behavior.
- P2: the canonical orchestration decision and execution sequence still carried Phase 5 status. Fixed by advancing maintained status and consequences to Phase 6 release-gated local proof.

All three CE review lenses cleared the fixes with no remaining P0-P2 findings.

## Independent Final Review

A fresh read-only reviewer inspected the complete diff against Phase 5 commit `084cfbbb0038269102f7eeb7a505f053dd0b4bc0` and the PLUXX-329 plan. It found no remaining P0-P2 issue and judged the Phase 0-6 stack locally ready for PR packaging.

One P3 residual remains: replay children rely on the surrounding CI/job timeout rather than a per-child timeout. A stalled child fails by hanging rather than passing; adding a bounded child timeout is optional hardening and is not a release blocker.

## Validation Evidence

- `npm run core-four:proof`: green with 44 pinned rows, 12 receipts, 176 adjunct outcomes, 324/324 degraded outcomes, current-registry migration compatibility, and two clean 12-case symlink/copy replays.
- focused release-proof, CLI, ownership, receipt, source-audit, compatibility, verifier, publish, and failure-injection suites: green.
- `npm test`: 70 files and 845 tests green.
- `npm run release:check`: green end-to-end, including proof freshness, build/declarations, typecheck, full tests, installed-package runtime verification, and dry-run pack.
- built CLI `lint`, `doctor`, and non-mutating `publish --dry-run`: green; expected core-four degradation warnings remain honest.
- generated compatibility output: current with no diff.
- `git diff --check`: green.
- privacy and network-dependence scans: no sensitive-state or mutable-network dependency in the new gate.

## Residual Boundary

This proves hermetic release readiness for isolated generated and installed topology, ownership, receipts, and registry compatibility. It does not prove real-host discovery, activation, or behavior, and it does not expand to any secondary host. Production and live plugin state remain untouched.
