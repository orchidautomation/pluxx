---
description: Validate and publish docs changes deliberately
argument-hint: "[changed-pages|page|release]"
---

Use the `docs-publish` skill.

Arguments: $ARGUMENTS

Run the bundled checks before recommending a publish.
