---
description: Inspect a docs surface and identify the right page or section before editing
argument-hint: "[topic-or-section]"
---

Use the `docs-inspect-surface` skill.

Arguments: $ARGUMENTS

Focus on:

1. mapping the docs surface
2. finding the most relevant page or section
3. identifying setup/auth/workflow gaps before any rewrite

If the user did not specify a page, return the best candidate pages and explain why.
