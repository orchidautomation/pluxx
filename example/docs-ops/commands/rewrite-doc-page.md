---
description: Rewrite a docs page with better product, setup, and workflow context
argument-hint: "[page-slug-or-url]"
---

Use the `docs-rewrite-page` skill.

Arguments: $ARGUMENTS

Before rewriting:

1. inspect the current page
2. load the bundled style references when needed
3. preserve product truth over writing style
