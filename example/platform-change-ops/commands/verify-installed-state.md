---
description: Verify the generated plugin, installed bundle, and runtime payload are still healthy in the selected host.
argument-hint: [host-or-plugin]
---

Use the `verify-installed-state` skill.

Arguments: `$ARGUMENTS`
