---
description: Gate a docs publish or release step only after evidence and approvals are in place.
argument-hint: [scope]
agent: release-reviewer
subtask: true
---

Use the `publish-docs-or-release` skill.

Arguments: `$ARGUMENTS`
