---
description: Inspect the repo, service, runbooks, and incidents that define the current blast radius.
argument-hint: [repo-or-service]
agent: release-reviewer
subtask: true
---

Use the `inspect-change-surface` skill.

Arguments: `$ARGUMENTS`
