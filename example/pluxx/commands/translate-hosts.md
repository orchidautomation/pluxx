---
description: Review preserve, translate, degrade, and drop behavior across the core four
argument-hint: "[surface or target optional]"
---

Use the Pluxx host translation workflow.

Arguments: $ARGUMENTS

## What To Do

1. Use the `pluxx-translate-hosts` skill.
2. Treat the argument as a surface or target focus when present.
3. Review only as deeply as needed for the user’s goal:
   - skills
   - commands
   - agents
   - hooks
   - permissions
   - runtime
   - distribution
4. Return the preserve/translate/degrade/drop truth and the highest-value source-shape fix.
