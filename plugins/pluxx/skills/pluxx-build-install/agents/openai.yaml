interface:
  display_name: "Build & Install"
  short_description: "Build installable plugins and install the one you want to test"
  default_prompt: "Use Pluxx to build installable plugins for Claude, Cursor, Codex, and OpenCode, then install the target I want to test locally."
