interface:
  display_name: "Rewrite Instructions"
  short_description: "Rewrite INSTRUCTIONS.md so the scaffold explains itself clearly"
  default_prompt: "Use the Pluxx instructions pack to rewrite the generated instructions so the plugin is clear and product-shaped."
