---
title: Core-Four Primitive Matrix
---

This page defines how the Pluxx authoring model should map across the core four hosts:

- Claude Code
- Cursor
- Codex
- OpenCode

The goal is not to copy one host's files into the others.

The goal is to normalize author intent once, then compile it into the best native surface each host exposes.

## Compiler Buckets

These are the nine canonical buckets Pluxx compiles from. All nine have current core-four mappings and Phase 5 receipts bound to pinned adjunct inventory and install ownership; activation and behavioral execution remain unproved:

| Bucket | Includes |
|---|---|
| `instructions` | shared host guidance and routing rules |
| `skills` | reusable workflows plus taxonomy-derived workflow metadata |
| `commands` | explicit slash-command style entrypoints |
| `agents` | specialist or delegated execution surfaces |
| `orchestration` | workflow activation, graph dispatch, artifact/state flow, bounded control, resume, and proof |
| `hooks` | deterministic lifecycle automation |
| `permissions` | portable `allow` / `ask` / `deny` intent |
| `runtime` | MCP, runtime auth, runtime readiness, local runtimes, passthrough dirs, helper scripts |
| `distribution` | user config, brand, packaging, install, publish, and install-surface metadata |

`taxonomy` remains an internal compiler input. It drives how skills, commands, instructions, agents, and orchestration are rendered, but it is not itself a host-facing extension primitive.

## Canonical Shape

```ts
interface PluxxPrimitiveModel {
  instructions?: InstructionsSpec
  skills?: SkillSpec[]
  commands?: CommandSpec[]
  agents?: AgentSpec[]
  orchestration?: OrchestrationSpec
  hooks?: HookSpec[]
  permissions?: PermissionSpec[]
  runtime?: {
    mcp?: {
      servers?: McpSpec
      auth?: RuntimeAuthSpec[]
    }
    readiness?: RuntimeReadinessSpec[]
    payload?: {
      passthrough?: string[]
      scripts?: string[]
      assets?: string[]
    }
  }
  distribution?: {
    identity?: IdentitySpec
    branding?: BrandSpec
    install?: {
      userConfig?: UserConfigSpec[]
      surface?: InstallSurfaceSpec
    }
    output?: {
      targets?: TargetPlatform[]
      outDir?: string
    }
    publish?: PublishSurfaceSpec
  }
  taxonomy: TaxonomySpec
}
```

This is a compiler-facing model. It is not a claim that each host has one identical manifest or one identical config file.

Two public buckets already need finer internal seams:

- `runtime` is MCP/auth, readiness, and generic bundled payload support
- `distribution` is identity/branding, install-time config surface, and build/output surface

## Mapping Rules

<!-- pluxx:core-four-primitives:start -->
<!-- Generated by `npm run generate:compatibility`. Do not edit this block by hand. -->

| Bucket | Claude Code | Cursor | Codex | OpenCode |
|---|---|---|---|---|
| `instructions` | `preserve` -> CLAUDE.md | `preserve` -> rules/, AGENTS.md | `preserve` -> AGENTS.md, AGENTS.override.md | `translate` -> AGENTS.md, CLAUDE.md, config instructions, plugin code |
| `skills` | `preserve` -> skills/<skill>/SKILL.md, skill frontmatter hooks | `degrade` -> skills/<skill>/SKILL.md, skills/<skill>/SKILL.md compatibility metadata, hooks/hooks.json | `degrade` -> skills/<skill>/SKILL.md, skills/<skill>/SKILL.md compatibility metadata, AGENTS.md, .codex/commands.generated.json, .codex/skills.generated.json, hooks/hooks.json | `degrade` -> skills/<skill>/SKILL.md, skills/<skill>/SKILL.md compatibility metadata, commands, plugin runtime guidance, skills.generated.json |
| `commands` | `preserve` -> commands/*.md, skills/<skill>/SKILL.md | `preserve` -> commands/*, slash commands | `degrade` -> skills/, AGENTS.md, .codex/commands.generated.json | `preserve` -> commands/*.md, config command definitions |
| `agents` | `preserve` -> agents/*.md | `translate` -> agents/, .cursor/agents/, ~/.cursor/agents/ | `translate` -> .codex/agents/*.toml, ~/.codex/agents/*.toml, subagent workflows | `preserve` -> agents/*.md, config agent definitions |
| `orchestration` | `degrade` -> claude-code-activation-guidance, claude-code-lifecycle-guidance, claude-code-workflow-graph-companion, claude-code-role-binding-guidance, claude-code-dispatch-guidance, claude-code-bounded-context-companion, claude-code-artifact-contract-companion, claude-code-state-contract-companion, claude-code-ownership-guidance, claude-code-barrier-guidance, claude-code-wait-guidance, claude-code-gate-guidance, claude-code-completion-guidance, claude-code-repair-guidance, claude-code-resume-ledger-guidance, claude-code-synthesis-guidance, claude-code-cancellation-guidance, claude-code-fallback-guidance, claude-code-proof-receipt-companion, claude-code-capability-guidance, claude-code-tool-guidance, claude-code-mcp-guidance, claude-code-permission-guidance, claude-code-sandbox-guidance, claude-code-credential-availability-guidance, claude-code-child-environment-guidance, claude-code-delegation-bounds-guidance | `degrade` -> cursor-activation-guidance, cursor-lifecycle-guidance, cursor-workflow-graph-companion, cursor-role-binding-guidance, cursor-dispatch-guidance, cursor-bounded-context-companion, cursor-artifact-contract-companion, cursor-state-contract-companion, cursor-ownership-guidance, cursor-barrier-guidance, cursor-wait-guidance, cursor-gate-guidance, cursor-completion-guidance, cursor-repair-guidance, cursor-resume-ledger-guidance, cursor-synthesis-guidance, cursor-cancellation-guidance, cursor-fallback-guidance, cursor-proof-receipt-companion, cursor-capability-guidance, cursor-tool-guidance, cursor-mcp-guidance, cursor-permission-guidance, cursor-sandbox-guidance, cursor-credential-availability-guidance, cursor-child-environment-guidance, cursor-delegation-bounds-guidance | `degrade` -> codex-activation-guidance, codex-lifecycle-guidance, codex-workflow-graph-companion, codex-role-binding-guidance, codex-dispatch-guidance, codex-bounded-context-companion, codex-artifact-contract-companion, codex-state-contract-companion, codex-ownership-guidance, codex-barrier-guidance, codex-wait-guidance, codex-gate-guidance, codex-completion-guidance, codex-repair-guidance, codex-resume-ledger-guidance, codex-synthesis-guidance, codex-cancellation-guidance, codex-fallback-guidance, codex-proof-receipt-companion, codex-capability-guidance, codex-tool-guidance, codex-mcp-guidance, codex-permission-guidance, codex-sandbox-guidance, codex-credential-availability-guidance, codex-child-environment-guidance, codex-delegation-bounds-guidance | `degrade` -> opencode-activation-guidance, opencode-lifecycle-guidance, opencode-workflow-graph-companion, opencode-role-binding-guidance, opencode-dispatch-guidance, opencode-bounded-context-companion, opencode-artifact-contract-companion, opencode-state-contract-companion, opencode-ownership-guidance, opencode-barrier-guidance, opencode-wait-guidance, opencode-gate-guidance, opencode-completion-guidance, opencode-repair-guidance, opencode-resume-ledger-guidance, opencode-synthesis-guidance, opencode-cancellation-guidance, opencode-fallback-guidance, opencode-proof-receipt-companion, opencode-capability-guidance, opencode-tool-guidance, opencode-mcp-guidance, opencode-permission-guidance, opencode-sandbox-guidance, opencode-credential-availability-guidance, opencode-child-environment-guidance, opencode-delegation-bounds-guidance |
| `hooks` | `degrade` -> hooks/hooks.json, settings hooks, frontmatter hooks | `degrade` -> hooks/hooks.json, .cursor/hooks.json | `degrade` -> hooks/hooks.json, .codex/hooks.json, generated command wrappers | `degrade` -> plugin JS/TS event handlers, plugin runtime event filters, plugin runtime event handlers |
| `permissions` | `translate` -> agent frontmatter, hook decisions, runtime approvals | `translate` -> hooks allow/deny, .cursor/cli.json, ~/.cursor/cli-config.json, subagent tool access | `translate` -> approvals, sandbox policy, hook matchers, custom agent config | `preserve` -> config permission, per-agent overrides |
| `runtime` | `preserve` -> .mcp.json, .claude-plugin/plugin.json, scripts/, assets/ | `preserve` -> mcp.json, .cursor/mcp.json, ~/.cursor/mcp.json, .cursor-plugin/plugin.json, scripts/, assets/ | `preserve` -> .mcp.json, .app.json, .codex/config.toml, scripts/, assets/ | `preserve` -> opencode.json, config mcp, plugin JS/TS runtime, scripts/, assets/ |
| `distribution` | `translate` -> .claude-plugin/plugin.json, marketplaces, install scopes, user configuration, /reload-plugins | `preserve` -> .cursor-plugin/plugin.json, .cursor-plugin/marketplace.json, local marketplace install path, reload window / restart | `preserve` -> .codex-plugin/plugin.json, ~/.agents/plugins/marketplace.json, $REPO_ROOT/.agents/plugins/marketplace.json, cache install path, restart after update | `translate` -> .opencode/plugins/, ~/.config/opencode/plugins/, opencode.json, npm package, plugin JS/TS entrypoint |

Registry notes:

#### `instructions`

- Claude Code: Claude has a native persistent instructions surface and plugin-level guidance model.
- OpenCode: OpenCode instructions are native, but the surface is config- and code-driven rather than manifest markdown only.

#### `skills`

- Claude Code: Derived from 18 audited skills fields; all preserve on claude-code.
- Cursor: Derived from field outcomes. Weaker fields: when_to_use=degrade, argument-hint=degrade, arguments=degrade, user-invocable=degrade, paths=degrade, shell=degrade, allowed-tools=degrade, model=degrade, effort=degrade, context=degrade, agent=degrade, hooks=degrade.
- Codex: Derived from field outcomes. Weaker fields: when_to_use=degrade, argument-hint=degrade, arguments=degrade, user-invocable=degrade, paths=degrade, shell=degrade, allowed-tools=degrade, model=degrade, effort=degrade, context=degrade, agent=degrade, hooks=degrade.
- OpenCode: Derived from field outcomes. Weaker fields: when_to_use=degrade, argument-hint=degrade, arguments=degrade, user-invocable=degrade, paths=degrade, shell=degrade, allowed-tools=degrade, model=degrade, effort=degrade, context=degrade, agent=degrade, hooks=degrade.

#### `commands`

- Claude Code: Claude still supports command files, but the product is increasingly converging command workflows into skills.
- Codex: Current Codex docs do not document plugin-packaged slash-command parity, so Pluxx keeps canonical command intent through routing guidance plus a generated companion mirror.

#### `agents`

- Claude Code: Claude plugin agents are a first-class native surface under plugin-root agents/. Plugin-shipped agents support a documented subset of agent frontmatter; unsupported fields such as color are omitted.
- Cursor: Cursor plugin agents are a first-class native surface under plugin-root agents/. Pluxx emits native readonly and is_background fields when canonical intent maps cleanly, then uses generated notes for richer fields.
- Codex: Codex plugins do not natively register plugin-local `.codex/agents/*.toml`. Pluxx treats generated agent TOML as install-managed companion state: install/apply syncs it into the active `CODEX_HOME/agents/<plugin>/`, verify checks that registration, and uninstall removes only unchanged Pluxx-owned files. Codex custom agents and subagents are real native surfaces, but they are not packaged the same way as Claude or Cursor plugin agents. Local May 13, 2026 headless probes prove explicit invocation, built-in-name override, project-local precedence, and discovered `.agents/skills` inheritance. The same maintained headless suite also showed two config-depth caveats: a parent `[[skills.config]] enabled = false` entry did not disable a discovered project skill, and an agent-local `[[skills.config]]` entry did not preload an undiscovered `skills/` path. The maintained `bun scripts/probe-codex-mcp-runtime.ts --json` headless probe was rerun on June 24, 2026: default project-scoped and user-scoped root MCP both emit a real `mcp_tool_call` item but fail with `user cancelled MCP tool call` before server-side `tools/call`; explicit `[mcp_servers.<id>.tools.<tool>] approval_mode = "approve"` unlocks project-scoped and user-scoped root MCP; custom agents that inherit those approved root MCP servers also reach `tools/call` and return `MCP_PROOF_MARKER_ALLOWED`. The June 24 run also shows that `mcp_servers = {}` in the child agent does not opt out of approved inherited root MCP for either project or user scope, while agent-local inline MCP and agent-local inline approval did not activate. Current custom-agent MCP success now surfaces a parent `mcp_tool_call` item in the `codex exec --json` stream, but `codex mcp list` still sees user-scoped servers and not project-scoped servers. The maintained `bun scripts/probe-codex-agents-interactive-runtime.ts --json` trusted interactive probe also showed the same `sandbox_mode = "read-only"` child agent still wrote to the workspace there too, so these fields are not yet uniformly trustworthy runtime boundaries.
- OpenCode: OpenCode agents are first-class native surfaces. Pluxx plugins register generated definitions through the plugin config hook. Prefer permission-first agent config and the native top_p key; legacy tools remains compatibility input.

#### `orchestration`

- Claude Code: Derived from 27 canonical fields in the orchestration registry. Effective outcomes retain explicit degradation until mechanism-specific behavioral evidence validates an executable host path.
- Cursor: Derived from 27 canonical fields in the orchestration registry. Effective outcomes retain explicit degradation until mechanism-specific behavioral evidence validates an executable host path.
- Codex: Derived from 27 canonical fields in the orchestration registry. Effective outcomes retain explicit degradation until mechanism-specific behavioral evidence validates an executable host path.
- OpenCode: Derived from 27 canonical fields in the orchestration registry. Effective outcomes retain explicit degradation until mechanism-specific behavioral evidence validates an executable host path.

#### `hooks`

- Claude Code: Derived from field outcomes. Weaker fields: prompt=degrade, failClosed=drop, loop_limit=drop.
- Cursor: Derived from field outcomes. Weaker fields: event=degrade, http=drop, mcp_tool=drop, agent=drop, loop_limit=degrade.
- Codex: Derived from field outcomes. Weaker fields: event=degrade, command=translate, prompt=drop, http=drop, mcp_tool=drop, agent=drop, matcher=degrade, failClosed=drop, loop_limit=drop, timeout=translate.
- OpenCode: Derived from field outcomes. Weaker fields: event=translate, command=translate, prompt=drop, http=drop, mcp_tool=drop, agent=drop, matcher=translate, loop_limit=drop, timeout=translate.

#### `permissions`

- Claude Code: Permission intent exists, but not as one single canonical plugin-level object.
- Cursor: Cursor can express permission intent, but it is spread across hooks, CLI permissions, and subagent configuration.
- Codex: Codex expresses permission intent through approvals, sandboxing, hooks, and custom agents rather than skill frontmatter. Pluxx now also emits `.codex/config.generated.toml` for the live-proven top-level MCP allow-path when canonical `MCP(...)` rules are concrete enough to materialize per-tool `approval_mode = "approve"` stanzas, while `.codex/permissions.generated.json` remains the broader advisory mirror.
- OpenCode: OpenCode permission is keyed by tool name and patterns, including native skill and task controls. Legacy tools booleans are deprecated in favor of permission.

#### `runtime`

- Codex: Bundle-local MCP config exists, but active MCP state also lives in config.toml. Local June 24, 2026 headless Codex MCP probes reached startup plus `tools/list` for project-scoped and user-scoped root config. Default root MCP emitted a real `mcp_tool_call` item but failed with `user cancelled MCP tool call` before server-side `tools/call`. Explicit per-tool `approval_mode = "approve"` now proves four concrete root/inheritance paths: project-scoped root MCP, user-scoped root MCP, a custom agent inheriting an approved project-scoped root MCP server, and a custom agent inheriting an approved user-scoped root MCP server. The same run also proves that an explicit child-agent `mcp_servers = {}` does not opt out of those approved inherited root MCP servers. Agent-local inline MCP and agent-local inline approval did not activate in the June 24 run, so least-privilege delegated MCP still belongs in the gap column. Project-scoped servers still do not appear in `codex mcp list`, while user-scoped servers do. Codex readiness now bundles translated hooks in the plugin, and Pluxx still emits `.codex/readiness.generated.json` plus `.codex/hooks.generated.json` as debugging companions because hook activation still depends on `[features].hooks = true`, enabled plugin state, review, trust, and runtime support. `codex_hooks` is deprecated and should not be treated as the current hook feature key.

#### `distribution`

- Claude Code: Distribution surfaces are native, including plugin marketplaces and explicit reload behavior.
- OpenCode: Distribution is native, but there is no single shared manifest analog to Claude, Cursor, or Codex.

## Field-level translation truth

Primitive labels above are derived from these audited field outcomes. A primitive is only `preserve` when every audited field preserves.

### Audited `skills` field outcomes

| Field | Claude Code | Cursor | Codex | OpenCode |
|---|---|---|---|---|
| `name` | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md |
| `description` | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md |
| `license` | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md |
| `compatibility` | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md |
| `metadata` | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md |
| `disable-model-invocation` | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md | `preserve` -> skills/<skill>/SKILL.md |
| `when_to_use` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, AGENTS.md, .codex/commands.generated.json | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, commands, plugin runtime guidance |
| `argument-hint` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata |
| `arguments` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata |
| `user-invocable` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata |
| `paths` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata |
| `shell` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata |
| `allowed-tools` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, .codex/skills.generated.json | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, skills.generated.json |
| `model` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, .codex/skills.generated.json | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, skills.generated.json |
| `effort` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, .codex/skills.generated.json | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, skills.generated.json |
| `context` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, .codex/skills.generated.json | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, skills.generated.json |
| `agent` | `preserve` -> skills/<skill>/SKILL.md | `degrade` -> skills/<skill>/SKILL.md compatibility metadata | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, .codex/skills.generated.json | `degrade` -> skills/<skill>/SKILL.md compatibility metadata, skills.generated.json |
| `hooks` | `preserve` -> skill frontmatter hooks | `degrade` -> hooks/hooks.json | `degrade` -> hooks/hooks.json | `degrade` -> skills/<skill>/SKILL.md compatibility metadata |

### Audited `hooks` field outcomes

| Field | Claude Code | Cursor | Codex | OpenCode |
|---|---|---|---|---|
| `event` | `preserve` -> hooks/hooks.json, settings hooks, frontmatter hooks | `degrade` -> hooks/hooks.json, .cursor/hooks.json | `degrade` -> hooks/hooks.json, .codex/hooks.json | `translate` -> plugin JS/TS event handlers |
| `command` | `preserve` -> hooks/hooks.json | `preserve` -> hooks/hooks.json | `translate` -> hooks/hooks.json, generated command wrappers | `translate` -> plugin JS/TS event handlers |
| `prompt` | `degrade` -> hooks/hooks.json, frontmatter hooks | `preserve` -> hooks/hooks.json | `drop` -> omitted | `drop` -> omitted |
| `http` | `preserve` -> hooks/hooks.json | `drop` -> omitted | `drop` -> omitted | `drop` -> omitted |
| `mcp_tool` | `preserve` -> hooks/hooks.json | `drop` -> omitted | `drop` -> omitted | `drop` -> omitted |
| `agent` | `preserve` -> hooks/hooks.json | `drop` -> omitted | `drop` -> omitted | `drop` -> omitted |
| `matcher` | `preserve` -> hooks/hooks.json | `preserve` -> hooks/hooks.json | `degrade` -> hooks/hooks.json | `translate` -> plugin runtime event filters |
| `failClosed` | `drop` -> omitted | `preserve` -> hooks/hooks.json | `drop` -> omitted | `preserve` -> plugin runtime event handlers |
| `loop_limit` | `drop` -> omitted | `degrade` -> hooks/hooks.json | `drop` -> omitted | `drop` -> omitted |
| `timeout` | `preserve` -> hooks/hooks.json | `preserve` -> hooks/hooks.json | `translate` -> hooks/hooks.json | `translate` -> plugin runtime event handlers |

### Audited `orchestration` field outcomes

Phase 5 maintains 12 validated deterministic `fake-home-install` receipts under `tests/fixtures/orchestration-runtime-receipts`, now bound to 176 pinned fixture adjunct inventory rows and install ownership. Exact source identity, revision, digest, host outcome policy, compiler output digest, and receipt digest are validated for Claude Code, Cursor, Codex, and OpenCode. Generated registration artifacts remain isolated evidence only: real-host discovery is environment-unavailable in all 12 cases, activation is unsupported, behavioral evidence is environment-unavailable, and all 324 orchestration outcomes remain explicit degradation.

| Field | Claude Code | Cursor | Codex | OpenCode |
|---|---|---|---|---|
| `activation-routing` | `degrade` -> claude-code-activation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Canonical intent routing does not contain the executable host entrypoint needed for native wiring. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-activation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Canonical intent routing does not contain the executable host entrypoint needed for native wiring. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-activation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Canonical intent routing does not contain the executable host entrypoint needed for native wiring. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-activation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Canonical intent routing does not contain the executable host entrypoint needed for native wiring. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `lifecycle-reentry` | `degrade` -> claude-code-lifecycle-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Lifecycle events are represented, but the IR does not contain an executable hook or extension adapter. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-lifecycle-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Lifecycle events are represented, but the IR does not contain an executable hook or extension adapter. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-lifecycle-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Lifecycle events are represented, but the IR does not contain an executable hook or extension adapter. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-lifecycle-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Lifecycle events are represented, but the IR does not contain an executable hook or extension adapter. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `workflow-graph` | `degrade` -> claude-code-workflow-graph-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>No core-four host consumes the canonical graph as a native declarative workflow. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-workflow-graph-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>No core-four host consumes the canonical graph as a native declarative workflow. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-workflow-graph-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>No core-four host consumes the canonical graph as a native declarative workflow. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-workflow-graph-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>No core-four host consumes the canonical graph as a native declarative workflow. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `role-binding` | `degrade` -> claude-code-role-binding-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Role references remain guidance because prompt assets cannot be projected into a runnable identity from this field alone. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-role-binding-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Role references remain guidance because prompt assets cannot be projected into a runnable identity from this field alone. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-role-binding-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Role references remain guidance because prompt assets cannot be projected into a runnable identity from this field alone. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-role-binding-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Role references remain guidance because prompt assets cannot be projected into a runnable identity from this field alone. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `generic-dispatch` | `degrade` -> claude-code-dispatch-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>The hosts expose dispatch primitives, but invocation remains prompt-driven rather than generated enforcement. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-dispatch-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>The hosts expose dispatch primitives, but invocation remains prompt-driven rather than generated enforcement. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-dispatch-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>The hosts expose dispatch primitives, but invocation remains prompt-driven rather than generated enforcement. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-dispatch-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>The hosts expose dispatch primitives, but invocation remains prompt-driven rather than generated enforcement. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `bounded-context` | `degrade` -> claude-code-bounded-context-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Packet limits are retained in the companion but are not enforced by a generated host boundary. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-bounded-context-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Packet limits are retained in the companion but are not enforced by a generated host boundary. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-bounded-context-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Packet limits are retained in the companion but are not enforced by a generated host boundary. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-bounded-context-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Packet limits are retained in the companion but are not enforced by a generated host boundary. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `artifact-flow` | `degrade` -> claude-code-artifact-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Artifact contracts are preserved as guidance without a native typed dataflow engine. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-artifact-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Artifact contracts are preserved as guidance without a native typed dataflow engine. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-artifact-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Artifact contracts are preserved as guidance without a native typed dataflow engine. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-artifact-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Artifact contracts are preserved as guidance without a native typed dataflow engine. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `state-flow` | `degrade` -> claude-code-state-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>State contracts are preserved as guidance without a native state engine. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-state-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>State contracts are preserved as guidance without a native state engine. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-state-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>State contracts are preserved as guidance without a native state engine. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-state-contract-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>State contracts are preserved as guidance without a native state engine. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `ownership` | `degrade` -> claude-code-ownership-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>File ownership remains advisory because no generated host policy enforces exclusive paths. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-ownership-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>File ownership remains advisory because no generated host policy enforces exclusive paths. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-ownership-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>File ownership remains advisory because no generated host policy enforces exclusive paths. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-ownership-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>File ownership remains advisory because no generated host policy enforces exclusive paths. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `barrier` | `degrade` -> claude-code-barrier-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Barrier semantics remain advisory because no generated host scheduler consumes them. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-barrier-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Barrier semantics remain advisory because no generated host scheduler consumes them. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-barrier-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Barrier semantics remain advisory because no generated host scheduler consumes them. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-barrier-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Barrier semantics remain advisory because no generated host scheduler consumes them. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `wait` | `degrade` -> claude-code-wait-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Wait predicates are rendered as guidance rather than executable host conditions. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-wait-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Wait predicates are rendered as guidance rather than executable host conditions. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-wait-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Wait predicates are rendered as guidance rather than executable host conditions. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-wait-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Wait predicates are rendered as guidance rather than executable host conditions. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `gate` | `degrade` -> claude-code-gate-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Required gates are visible but not mechanically host-enforced. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-gate-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Required gates are visible but not mechanically host-enforced. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-gate-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Required gates are visible but not mechanically host-enforced. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-gate-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Required gates are visible but not mechanically host-enforced. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `completion` | `degrade` -> claude-code-completion-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Completion predicates are visible but not mechanically host-enforced. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-completion-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Completion predicates are visible but not mechanically host-enforced. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-completion-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Completion predicates are visible but not mechanically host-enforced. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-completion-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Completion predicates are visible but not mechanically host-enforced. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `repair` | `degrade` -> claude-code-repair-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Targeted repair policy is visible but not mechanically host-enforced. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-repair-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Targeted repair policy is visible but not mechanically host-enforced. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-repair-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Targeted repair policy is visible but not mechanically host-enforced. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-repair-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Targeted repair policy is visible but not mechanically host-enforced. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `resume` | `degrade` -> claude-code-resume-ledger-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Resume and dedupe state require a durable runtime adapter that Phase 2 does not install. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-resume-ledger-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Resume and dedupe state require a durable runtime adapter that Phase 2 does not install. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-resume-ledger-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Resume and dedupe state require a durable runtime adapter that Phase 2 does not install. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-resume-ledger-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Resume and dedupe state require a durable runtime adapter that Phase 2 does not install. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `synthesis` | `degrade` -> claude-code-synthesis-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Synthesis ownership is prompt-guided rather than bound to a generated host primitive. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-synthesis-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Synthesis ownership is prompt-guided rather than bound to a generated host primitive. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-synthesis-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Synthesis ownership is prompt-guided rather than bound to a generated host primitive. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-synthesis-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Synthesis ownership is prompt-guided rather than bound to a generated host primitive. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `cancellation` | `degrade` -> claude-code-cancellation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Cancellation policy is prompt-guided rather than bound to a generated host primitive. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-cancellation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Cancellation policy is prompt-guided rather than bound to a generated host primitive. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-cancellation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Cancellation policy is prompt-guided rather than bound to a generated host primitive. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-cancellation-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Cancellation policy is prompt-guided rather than bound to a generated host primitive. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `fallback` | `degrade` -> claude-code-fallback-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Fallback order is retained as guidance without an executable adapter. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-fallback-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Fallback order is retained as guidance without an executable adapter. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-fallback-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Fallback order is retained as guidance without an executable adapter. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-fallback-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Fallback order is retained as guidance without an executable adapter. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `proof-requirements` | `degrade` -> claude-code-proof-receipt-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Proof requirements are emitted into the receipt but not executed against an installed host. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-proof-receipt-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Proof requirements are emitted into the receipt but not executed against an installed host. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-proof-receipt-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Proof requirements are emitted into the receipt but not executed against an installed host. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-proof-receipt-companion<br/>activation: external-adapter; enforcement: prompt-guided; child environment: not-applicable; evidence: unit<br/>Proof requirements are emitted into the receipt but not executed against an installed host. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `capability-requirements` | `degrade` -> claude-code-capability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Capability needs are listed without a generated host admission check. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-capability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Capability needs are listed without a generated host admission check. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-capability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Capability needs are listed without a generated host admission check. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-capability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Capability needs are listed without a generated host admission check. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `tool-requirements` | `degrade` -> claude-code-tool-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Tool needs are listed without a generated host admission check. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-tool-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Tool needs are listed without a generated host admission check. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-tool-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Tool needs are listed without a generated host admission check. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-tool-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Tool needs are listed without a generated host admission check. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `mcp-requirements` | `degrade` -> claude-code-mcp-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>MCP needs are listed without changing the separately owned MCP configuration. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-mcp-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>MCP needs are listed without changing the separately owned MCP configuration. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-mcp-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>MCP needs are listed without changing the separately owned MCP configuration. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-mcp-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>MCP needs are listed without changing the separately owned MCP configuration. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `permission-requirements` | `degrade` -> claude-code-permission-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Permission needs are listed without widening or rewriting separately owned policy. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-permission-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Permission needs are listed without widening or rewriting separately owned policy. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-permission-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Permission needs are listed without widening or rewriting separately owned policy. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-permission-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Permission needs are listed without widening or rewriting separately owned policy. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `sandbox-requirements` | `degrade` -> claude-code-sandbox-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Sandbox minimums are listed without claiming host enforcement. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-sandbox-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Sandbox minimums are listed without claiming host enforcement. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-sandbox-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Sandbox minimums are listed without claiming host enforcement. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-sandbox-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Sandbox minimums are listed without claiming host enforcement. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `credential-availability` | `degrade` -> claude-code-credential-availability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Credential availability classes are listed without reading or embedding secret material. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-credential-availability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Credential availability classes are listed without reading or embedding secret material. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-credential-availability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Credential availability classes are listed without reading or embedding secret material. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-credential-availability-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Credential availability classes are listed without reading or embedding secret material. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `inheritance-override` | `degrade` -> claude-code-child-environment-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Child-environment override intent is listed without claiming the host can enforce every dimension. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-child-environment-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Child-environment override intent is listed without claiming the host can enforce every dimension. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-child-environment-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Child-environment override intent is listed without claiming the host can enforce every dimension. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-child-environment-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Child-environment override intent is listed without claiming the host can enforce every dimension. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |
| `delegation-bounds` | `degrade` -> claude-code-delegation-bounds-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Delegation and concurrency budgets are listed without claiming the host enforces all bounds. The claude-code generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> cursor-delegation-bounds-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Delegation and concurrency budgets are listed without claiming the host enforces all bounds. The cursor generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> codex-delegation-bounds-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Delegation and concurrency budgets are listed without claiming the host enforces all bounds. The codex generator therefore emits deterministic companion guidance and no native-enforcement claim. | `degrade` -> opencode-delegation-bounds-guidance<br/>activation: external-adapter; enforcement: prompt-guided; child environment: weakened; evidence: unit<br/>Delegation and concurrency budgets are listed without claiming the host enforces all bounds. The opencode generator therefore emits deterministic companion guidance and no native-enforcement claim. |

<!-- pluxx:core-four-primitives:end -->
## Practical Consequences

This matrix implies a few hard rules for Pluxx:

1. Migration should be semantic, not syntactic.
   Import the source host's intent, not its exact file format.

2. Skills are the stable workflow layer.
   Commands and agents should compile from the same workflow model rather than forking meaning.

3. Permissions are portable intent, not portable syntax.
   A Claude-only field such as `allowed-tools` should become a host-agnostic policy that Pluxx re-expresses for Cursor, Codex, and OpenCode in their native control planes.

4. Codex is not command-parity with Claude, Cursor, or OpenCode today.
   That is a translation problem for Pluxx, not a reason to leak host differences into the authoring model.

5. OpenCode is code-first for plugins.
   Hooks, runtime extensions, and some distribution behavior need real JS or TS generation rather than manifest-only generation.

6. Install and update behavior are part of compatibility truth, not just packaging trivia.
   The host story includes reload behavior, restart requirements, cache semantics, and marketplace or local install paths.

## Official Docs Basis

### Claude Code

- [Extend Claude Code](https://code.claude.com/docs/en/features-overview)
- [Create plugins](https://code.claude.com/docs/en/plugins)
- [Plugins reference](https://code.claude.com/docs/en/plugins-reference)
- [Hooks](https://code.claude.com/docs/en/hooks)
- [Create custom subagents](https://code.claude.com/docs/en/sub-agents)
- [How Claude Code works](https://code.claude.com/docs/en/how-claude-code-works)

### Cursor

- [Plugins](https://cursor.com/docs/plugins)
- [Agent Skills](https://cursor.com/docs/skills)
- [Subagents](https://cursor.com/docs/subagents)
- [Hooks](https://cursor.com/docs/hooks)
- [Rules](https://cursor.com/docs/rules)
- [MCP](https://cursor.com/docs/mcp)
- [Slash commands](https://cursor.com/docs/cli/reference/slash-commands)
- [Permissions](https://cursor.com/docs/cli/reference/permissions)

### Codex

- [Build plugins](https://developers.openai.com/codex/plugins/build)
- [Agent Skills](https://developers.openai.com/codex/skills)
- [Subagents](https://developers.openai.com/codex/subagents)
- [Hooks](https://developers.openai.com/codex/hooks)
- [Model Context Protocol](https://developers.openai.com/codex/mcp)
- [Custom instructions with AGENTS.md](https://developers.openai.com/codex/guides/agents-md)

### OpenCode

- [Config](https://opencode.ai/docs/config/)
- [Plugins](https://opencode.ai/docs/plugins/)
- [Skills](https://opencode.ai/docs/skills/)
- [Commands](https://opencode.ai/docs/commands/)
- [Agents](https://opencode.ai/docs/agents/)
